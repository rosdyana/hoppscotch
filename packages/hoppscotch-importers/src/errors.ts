/** Returned when a file does not match the importer's expected format. */
export const IMPORTER_INVALID_FILE_FORMAT = "importer_invalid_file_format"

export type HoppImporterError = typeof IMPORTER_INVALID_FILE_FORMAT
